import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    container: {
        width:"100%",
        borderWidth:1
    },
    title:{
        margin:20,
        marginTop:15
    },
    scrollView:{
        backgroundColor: "white"
    }
});